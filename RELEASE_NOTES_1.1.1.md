# 画境 1.1.1

本版本是 1.1.0 的修复版：把上一轮排查出的 11 个竞态、原子写入和进程管理问题全部修掉，
并补齐了可重复执行的前端回归测试。功能与界面与 1.1.0 一致，不需要重新配置。

## 修复

**提交与竞态**

- 修复视频首次生成可能直接失败：表单里的 Vue 响应式对象（视频能力、历史恢复的 2K 参数）
  进入任务请求时会触发 `DataCloneError: #<Object> could not be cloned.`。
  现在进入任务引擎前会建立只含普通值的显式快照。
- 修复启动后端与视频能力检测的竞态：并发的调用共享同一次启动/检测，
  第二个调用者会等到真正完成，不会再出现「ComfyUI 尚未运行」「工作流未就绪」这种自相矛盾的报错。
- 修复准备阶段切页后可重复提交：启动后端、检测工作流、上传首尾帧整个过程由 store 持有提交锁，
  组件卸载不会让锁消失；这期间图片任务也会被挡住，避免两边同时提交抢显存。
- 修复任务运行期间切页导致结果面板空白：结果由任务引擎在落盘成功后直接写入按模式分组的状态，
  不再依赖可能已经卸载的表单组件。
- 修复应用初始化期间可以过早输入、随后被恢复的数据覆盖：等设置、表单、历史恢复完成后再开放创作界面。

**取消与进程**

- 取消任务改为「确认停止」后才解锁：`/queue delete` 或定向 `/interrupt` 返回成功不等于任务停了，
  后端会继续按任务编号检查队列与历史，确认它真的离开队列（或进入 `execution_interrupted`）才返回；
  取消没确认成功时会继续跟踪原任务，不会让用户误以为可以再提交一次。
- 修复后端启动不检查子进程退出、重复启动覆盖状态：启动请求串行化，
  轮询期间发现子进程已退出会立刻带出日志原因；同一时刻只保留一个受管理的子进程。
- 取消只影响本次任务，其他客户端的任务不受影响（沿用 1.1.0 的定向取消）。

**文件与持久化**

- 修复历史/设置/图库等 JSON 并发保存互相覆盖：每次写入使用唯一临时文件并原子改名，
  同一份数据按队列串行写入；读到的永远是一份完整 JSON。
- 修复下载中断留下半截文件：结果先写到同目录临时文件，完整读完并确认非空后再原子改名，
  失败会清理临时文件；已存在的成果文件不会被覆盖。
- 修复隐私空间移入绕过历史写入队列的问题：移入、移出、删除统一走历史 store 的串行接口。

**日志**

- 修复日志跟随器在多字节 UTF-8 被读取块切断时出现乱码或丢行：按字节缓冲，
  只有完整的 UTF-8 序列才转成文本，读取位置按实际读到的字节推进。
- 修复日志文件含非 UTF-8 字节时整份读不出来（中文 Windows 上 Python 按本地代码页输出，
  tqdm 进度块会把日志变成非法 UTF-8）：启动 ComfyUI 时强制 UTF-8 输出，读取时按字节宽松解码。
  日志面板在应用重启后（内存缓冲为空、只能读文件）也能正常显示。

## 测试与验证

- 新增前端回归测试：`pnpm test:ui`，32 项，覆盖 Proxy 请求快照、并发启动、能力检测竞态、
  切页后的结果写入、取消等待、历史并发写入。不依赖额外测试框架，也不需要后端在跑。
- Rust 测试扩充到 92 项，新增：并发写入不留临时文件、读取不会看到半截 JSON、
  下载中断不留半截文件、取消确认与超时、跨读取块的 UTF-8 重组、非 UTF-8 日志容错。
- 本机联调（`cargo test --test comfy_live`，需要 ComfyUI 在跑）：
  真实启动后端（14 秒就绪）、H3 视频工作流依赖齐全、真实取消确认、
  真实结果下载（136 KB PNG，原子落盘、无残留）。

## 打包

安装版与便携版版本号均为 1.1.1，可直接覆盖安装 1.1.0，应用标识不变，
原有设置、图库、历史记录与隐私空间继续可用。
