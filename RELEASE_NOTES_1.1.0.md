# 画境 1.1.0

本版本新增本机 MiniMax H3 Turbo 帧生视频，并完成了创作界面、结果预览、历史复现和任务控制的整合。

## 新增

- 支持首帧或首尾帧生成视频，使用本机 ComfyUI，不调用云端接口。
- 提供横屏 864×480、竖屏 480×864、方形 640×640 三种画幅。
- 提供约 5 秒、10 秒、15 秒三档时长，统一输出 24 fps MP4，并保留模型生成的音轨。
- 自动检查 H3 节点和五个模型文件，缺少依赖时在界面中直接说明。
- 视频完成后可在应用内播放、打开所在目录、调用系统播放器或另存副本。
- 图片结果可一键设为视频首帧；视频任务和参数会进入历史记录并可恢复。

## 修复与易用性改进

- 取消任务只影响本次生成，避免中断其他 ComfyUI 客户端的任务。
- 取消过程中保持任务锁，避免重复提交造成显存争用。
- 修复同名参考图上传互相覆盖、同名结果导入覆盖旧文件的问题。
- 修复从结果跳转到编辑或放大时仍使用旧参考图的问题。
- 修复首次启动时用户默认参数可能未生效的问题。
- 历史记录补全参考图、2K 放大参数和视频参数；永久删除文件前增加明确确认。
- 删除失败时保留历史记录，避免界面显示已删除而磁盘文件仍存在。

## 本机工作流要求

默认连接 `E:\ComfyUI`，需要当前项目已使用的 H3 节点，以及以下模型：

- `minimax_h3_fl2va_pruned_int8_convrot.safetensors`
- `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors`
- `minimax_h3_video_vae_fp16.safetensors`
- `minimax_h3_audio_vae_fp32.safetensors`
- `minimax_h3_turbo_4step_ckpt500.safetensors`

应用会从 ComfyUI 实际提供的模型列表中寻找这些文件，因此模型放在兼容的子目录中也可以识别。

## 验证

- 已在当前本机 ComfyUI 上完成真实端到端生成：864×480、24 fps、5.167 秒、H.264 视频与 AAC 音轨。
- 前端生产构建通过。
- Rust 完整测试通过，共 62 项，覆盖真实 ComfyUI 往返、视频能力检测、精确取消、文件下载与重复文件保护。

安装版可直接覆盖旧版本，应用标识保持不变，原有设置、图库和历史记录会继续使用。
